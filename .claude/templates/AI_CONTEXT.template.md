# AI CONTEXT - {{PROJECT_NAME}}

*Optional additional project context for AI agents*

## Purpose

- Put stable project-specific guidance here that should not live in `CLAUDE.md` or `AGENTS.md`.
- Keep transient execution state in `.claude/*.md` files instead.

## Stable Guidance

- Domain terminology:
- Naming conventions:
- Non-obvious business rules:
- Integration caveats:
